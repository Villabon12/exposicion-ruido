import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'exposicion-ruido',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
